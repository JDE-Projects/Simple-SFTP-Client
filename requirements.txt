# Pinned build dependencies for Simple SFTP Client.
# Base versions (pywebview, PySide6, qtpy, pyinstaller) match the JDE pilot's
# green CI build (Python 3.14). SFTP stack pinned to current stable 2026-06-18.
# Dependabot opens review-PRs for these == deps when newer releases appear.
pywebview==6.2.1
PySide6==6.11.1
qtpy==2.4.3
pyinstaller==6.21.0

# SFTP transport + key handling. cryptography is used directly (Ed25519 keygen)
# as well as by paramiko; bcrypt and PyNaCl are paramiko's crypto deps.
paramiko==5.0.0
cryptography==49.0.0
bcrypt==5.0.0
PyNaCl==1.6.2

# Optional "remember password" via the OS keychain (Windows Credential Manager).
keyring==25.7.0
